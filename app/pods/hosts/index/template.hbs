{{#ap-page t='resources.host_plural' help-url=(docs-url 'hosts') showNew=true}}

  {{ap-table-auto-index
    model=model
    fields=fields
    route='host.index'
    edit-route='host.edit'
    show-delete=true}}

  {{#ap-panel-new t='resources.host' closed=(unbound model.length)}}
    {{forms/host-form indexModel=model}}
  {{/ap-panel-new}}

{{/ap-page}}
