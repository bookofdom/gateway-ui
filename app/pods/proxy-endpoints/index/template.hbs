{{#ap-page t='resources.proxy-endpoint_plural' help-url=(docs-url 'proxy-endpoints') showNew=true}}

  {{ap-table-auto-index
      model=model
      fields=fields
      route='proxy-endpoint.edit'}}

  {{#ap-panel-new t='resources.proxy-endpoint' closed=(unbound filtered.length)}}
    {{forms/proxy-endpoint-form indexModel=model option-groups=option-groups savedAction='saved'}}
  {{/ap-panel-new}}

{{/ap-page}}
