{{#ap-page t='resources.proxy-endpoint_plural' help-url=(docsUrl 'proxy-endpoints') showNew=true}}

  {{partial 'proxy-endpoints/filter-form'}}

  {{ap-table-auto-index
      model=filtered
      fields=fields
      route='proxy-endpoint.edit'}}

  {{#ap-panel-new t='resources.proxy-endpoint' closed=(unbound filtered.length)}}
    {{forms/proxy-endpoint-form indexModel=model option-groups=option-groups savedAction='saved'}}
  {{/ap-panel-new}}

{{/ap-page}}
