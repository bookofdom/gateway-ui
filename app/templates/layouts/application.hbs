<div class="ap-app">
  {{#if session.isAuthenticated}}
    {{partial 'layouts/application/header'}}
    {{partial 'layouts/application/toolbar'}}  
  {{/if}}

  {{outlet}}
</div>

{{ember-notify messageStyle='bootstrap' closeAfter=5000}}
