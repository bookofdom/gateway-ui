{{#ap-page t='resources.proxy-endpoint_plural' help-url='http://devhub.justapis.com/v4.1.1/docs/proxy-endpoints' showNew=true}}

  {{partial 'proxy-endpoints/-filter-form'}}

  {{ap-table-auto-index
      model=filtered
      fields=fields
      route='proxy-endpoint.edit'}}

  {{!--
  {{ap-table-index
      models=filtered
      fields='name,description,environment:resources.environment:object-name,endpoint_group:resources.endpoint-group:object-name,active::active-label'
      path='proxy-endpoint.index'
      edit-path='proxy-endpoint.edit'}}
  --}}

  {{#ap-panel-new t='resources.proxy-endpoint' closed=(unbound filtered.length)}}
    {{render 'proxy-endpoint.form'}}
  {{/ap-panel-new}}

{{/ap-page}}
