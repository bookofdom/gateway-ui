{{#each fieldsList}}
  {{#ap-model-form-group
      model=controller.model
      field=field
      inline=controller.inline
      horizontal=controller.horizontal}}
    {{ap-model-field
        model=controller.model
        field=field
        options=options
        prompt=prompt}}
  {{/ap-model-form-group}}
{{/each}}

{{yield}}

{{ap-form-actions inline=inline horizontal=horizontal show-save=dirty show-cancel=dirty}}
