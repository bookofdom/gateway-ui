{{#each fieldsList}}
  {{#ap-model-form-group
      model=controller.model
      field=field
      horizontal=controller.horizontal}}
    {{ap-model-field
        model=controller.model
        field=field
        options=options
        prompt=prompt}}
  {{/ap-model-form-group}}
{{/each}}

{{yield}}

{{ap-form-actions show-save=dirty show-cancel=dirty}}
