{{#bs-table headings=headings}}
  {{#each model in models}}
    {{#unless model.isNew}}
      {{ap-table-index-row
        model=model
        fields=fields
        path=path
        edit-path=edit-path}}
    {{/unless}}
  {{/each}}
{{/bs-table}}
