<div class="page-header clearfix">
  {{#if help-url}}
    {{ap-button-help url=help-url class='pull-right'}}
  {{/if}}
  {{#if t}}
    <h1>{{partial 'components/bs-text'}}</h1>
  {{/if}}
</div>

{{yield}}
