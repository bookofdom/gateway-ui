{{view 'select'
    name=name
    id=idName
    selection=value.content
    content=options
    optionValuePath='content.id'
    optionLabelPath='content.name'
    prompt=prompt
    required=required
    class='form-control'}}
