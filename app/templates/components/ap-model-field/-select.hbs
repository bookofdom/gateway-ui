{{view 'select'
    name=fieldName
    id=fieldName
    value=value
    content=options
    optionValuePath='content.value'
    optionLabelPath='content.name'
    prompt=prompt
    class='form-control'}}
