{{view 'select'
    name=fieldName
    id=idName
    value=value
    content=options
    optionValuePath='content.value'
    optionLabelPath='content.name'
    prompt=prompt
    class='form-control'}}
