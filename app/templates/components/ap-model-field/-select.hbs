{{view 'select'
    name=name
    id=idName
    value=value
    content=options
    optionValuePath='content.value'
    optionLabelPath='content.name'
    prompt=prompt
    required=required
    class='form-control'}}
